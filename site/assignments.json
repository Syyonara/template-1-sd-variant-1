{
  "$comment": "Template resolution. `defaults` names the channel default template per slot; `rules` attaches a template to a set of routes by display condition. An empty file means every slot falls back to the shipped default, which renders site/chrome/*.html — that is what a freshly provisioned repo looks like.",
  "defaults": {
    "utilityNav": null,
    "header": null,
    "footer": null,
    "siteFooter": null
  },
  "rules": []
}
